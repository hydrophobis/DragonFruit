package dragonfruit;

import dragonfruit.App.*;
import java.net.*;
import java.util.logging.LogRecord;
import java.io.IOException;

public class Interceptor{

    public static load(){
        Socket socket = new Socket();
        boolean status = java.net.ServerSocket.isConnected();
        socket.bind("192.86.101", 1085);
        Socket.accept();
        Log("Listening");

        if(status == true

        );
    }
}